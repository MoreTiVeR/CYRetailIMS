@{
    ViewData["Title"] = "InventoryTransfer";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="page-header">
    <div class="page-title">
        <h4>โอนสินค้า(ใหม่)</h4>
        <h6>ทำรายการโอนสินค้า</h6>
    </div>
    <div class="page-btn">
        <a asp-action="Create" asp-controller="Item" class="btn btn-added"><img src="~/assets/img/icons/plus.svg" alt="img" class="me-1">เพิ่มสินค้าใหม่</a>
    </div>
</div>

<!-- /product list -->
<div class="card">
    <div class="card-body">
        <div class="table-top">
            <div class="search-set">
                <div class="search-path">
                    <a class="btn btn-filter" id="filter_search">
                        <img src="~/assets/img/icons/filter.svg" alt="img">
                        <span><img src="~/assets/img/icons/closes.svg" alt="img"></span>
                    </a>
                </div>
                <div class="search-input">
                    <a class="btn btn-searchset"><img src="~/assets/img/icons/search-white.svg" alt="img"></a>
                </div>
            </div>
            @* <div class="wordset">
            <ul>
            <li>
            <a id="btnExportExcel" data-bs-toggle="tooltip" data-bs-placement="top" title="ดาวน์โหลดไฟล์ excel"><img src="~/assets/img/icons/excel.svg" alt="img"></a>
            </li>
            </ul>
            </div>*@
        </div>
        <!-- /Filter -->
        <div class="card mb-0" id="filter_inputs">
            <div class="card-body pb-0">
                <div class="row">
                    <div class="col-lg-3 col-sm-6 col-12">
                        <div class="form-group">
                            @* <select id="ddlBranch" class="select">
                            <option>เลือกสาขา</option>
                            @if (ViewBag.BranchList.result)
                            {
                            foreach (var item in ViewBag.BranchList.data)
                            {
                            <option value="@item.branchid">@item.branchname</option>
                            }
                            }
                            else
                            {
                            <option value="">--ไม่พบข้อมูล--</option>
                            }
                            </select> *@
                            <select id="ddlBranch" class="ddl-select2 select2 ddl-source-branch" asp-items="@(new SelectList(ViewBag.BranchList, "Value", "Text", null, null))">
                                <option value="">--เลือกสาขา--</option>
                            </select>
                        </div>
                    </div>
                    <div class="col-lg-3 col-sm-6 col-12">
                        @* <select id="ddlBranch" class="select">
                        <option>เลือกแบรนด์สินค้า</option>
                        @if (ViewBag.ItemBrandList.result)
                        {
                        foreach (var item in ViewBag.ItemBrandList.data)
                        {
                        <option value="@item.branchid">@item.branchname</option>
                        }
                        }
                        else
                        {
                        <option value="">--ไม่พบข้อมูล--</option>
                        }
                        </select> *@
                        <select id="ddlBrand" class="ddl-select2 select2 ddl-source-brand" asp-items="@(new SelectList(ViewBag.ItemBrandList, "Value", "Text", null, null))">
                            <option value="">--เลือกแบรนด์--</option>
                        </select>
                    </div>
                    <div class="col-lg-3 col-sm-6 col-12">
                        <div class="form-group" style="text-align: left">
                            <a id="btnSearch" class="btn btn-filters" title="กดเพิ่อค้นหา"><img src="~/assets/img/icons/search-whites.svg" alt="img"></a>
                        </div>
                    </div>
                    <div class="col-lg-3 col-sm-6 col-12">
                    </div>
                </div>
            </div>
        </div>
        <!-- /Filter -->
        <div class="table-responsive">
            <table id="tbItemInventoryTransfer" class="table">
                <thead>
                    <tr>
                        <th>
                            <label class="checkboxs">
                                <input type="checkbox" id="select-all">
                                <span class="checkmarks"></span>
                            </label>
                        </th>
                        <th hidden>ไอดีสินค้า</th>
                        <th>รหัสสินค้า</th>
                        <th>ชื่อสินค้า</th>
                        <th>คลังคงเหลือ</th>
                        <th>คงเหลือหน้าร้าน</th>
                        <th>จำนวนขั้นต่ำ</th>
                        <th>จำนวนที่ต้องเติม</th>
                        <th>จำนวน/แก้ไขได้</th>
                    </tr>
                </thead>
                <tbody>
                </tbody>
            </table>
        </div>
        @* <div class="row">
            <div class="form-group">
                <a asp-action="Index" asp-controller="Item" class="btn btn-cancel me-2">ยกเลิก</a>
                <button id="btnAddItem" type="submit" class="btn btn-submit me-2">บันทึกข้อมูล</button>
            </div>
        </div> *@
    </div>
    <div class="card-body text-center">
            @* <button id="submit" type="submit">Submit form</button> *@
            <div class="form-group">
                @*<button id="btnCancel" type="reset" class="btn btn-cancel me-2">ยกเลิก</button>*@
                <a asp-action="Index" asp-controller="Item" class="btn btn-cancel me-2">บันทึกฉบับร่าง</a>
                <button id="btnConfirmTransfer" type="submit" class="btn btn-submit me-2">บันทึกข้อมูล</button>
            </div>
    </div>
</div>
<!-- /product list -->
@section Scripts {

    <script type="text/javascript" charset="utf8" src="https://cdn.datatables.net/buttons/2.4.2/js/dataTables.buttons.min.js"></script>
    <script type="text/javascript" charset="utf8" src="https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js"></script>
    <script type="text/javascript" charset="utf8" src="https://cdn.datatables.net/buttons/2.4.2/js/buttons.html5.min.js"></script>

    @* <script type="text/javascript" src="https://cdn.datatables.net/1.10.21/js/jquery.dataTables.min.js"></script> *@
    @* <script type="text/javascript" charset="utf8" src="https://cdn.jsdelivr.net/npm/datatables.net-editor@2.3.1/js/dataTables.editor.min.js"></script> *@
    @* <script type="text/javascript" charset="utf8" src="https://editor.datatables.net/extensions/Editor/js/editor.dataTables.js"></script> *@
    @* <script type="text/javascript" charset="utf8" src="~/lib/jquery-datatable-editable/jquery.jeditable.js"></script> *@
    @* <script type="text/javascript" charset="utf8" src="~/lib/jquery-tabledit-1.2.3/jquery.tabledit.js"></script> *@

    <script type="text/javascript" charset="utf8" src="~/lib/datatable-editor-2.3.2/js/dataTables.editor.js"></script>
    <script type="text/javascript" charset="utf8" src="~/lib/datatable-editor-2.3.2/js/editor.dataTables.js"></script>

    <!--Page Script-->
    <script src="~/js/view/item/item_inventorytransfer.js" asp-append-version="true"></script>
}

